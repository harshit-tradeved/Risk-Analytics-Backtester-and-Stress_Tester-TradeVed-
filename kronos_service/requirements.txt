# Kronos service dependencies
# Install after cloning the Kronos repo:
#   pip install -r Kronos/requirements.txt
#   pip install -r requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0       # used by engine/forecast.py KronosClient
pydantic>=2.0.0

# Kronos model deps (from Kronos/requirements.txt — pinned here for Railway/Modal)
torch>=2.0.0
einops==0.8.1
huggingface_hub==0.33.1
safetensors==0.6.2
tqdm>=4.67.0
pandas>=2.2.0
numpy>=1.24.0

# TradeVed Backtester - Dependencies (pinned for reproducible deploys)

# ── Web framework ──
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.29
slowapi==0.1.9

# ── Data / scientific stack ──
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
plotly==6.7.0
kaleido==1.3.0

# ── Persistence ──
sqlalchemy==2.0.50
pydantic==2.13.4

# ── HTTP ──
requests==2.31.0
aiohttp==3.9.3

# ── Market data sources ──
python-binance==1.0.36
pycoingecko==3.2.0
yfinance==1.4.0
fyers-apiv3==3.1.12

# ── Optional NSE intraday data sources ──
# Lazily imported in data/fetcher.py with graceful ImportError fallback — NOT
# required for the core API (Binance / CoinGecko / yfinance cover all markets).
# `openchart>=1.1.0` is not published for Linux, so it is omitted from deploys.
# Re-install locally if you need these sources:
#   openchart
#   tvdatafeed @ git+https://github.com/rongardF/tvdatafeed.git
